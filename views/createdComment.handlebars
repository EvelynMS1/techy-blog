<div
  class="comment"
  id="blogComment"
  data-id="{{comment.id}}"
  data-user_id="{{comment.user_id}}"
>
  <h1>{{comment.title}}</h1>
  <p>{{comment.comment}}</p>
  <p>Blog Content: {{blog.content}}</p>
</div>