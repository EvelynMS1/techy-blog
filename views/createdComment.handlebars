
<div>Created Comment</div>
<div
  class="comment"
  id="blogComment"
  data-id="{{comment.id}}"
  data-user_id="{{comment.user_id}}"
>
  <div>
    <h1>{{comment.title}}</h1>
    <p>{{comment.comment}}</p>
  </div>

</div>
{{>blog_template}}